## 0.1.0

- Initial release: `ArmenusClient`, `resolvePresentation()`, `ArmenusModel` with
  SceneKit and AR Quick Look on iOS, Filament (SceneView) and Scene Viewer on
  Android, `ArmenusAr.prefetch()` and an LRU disk cache.
- Not yet compiled or run against a Flutter toolchain. See the README status note.
